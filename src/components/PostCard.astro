---
import { Image } from 'astro:assets'

const {
	data: { title, description, issue, heroImage, category, alt, author, tags },
	readTime,
	slug,
	cover
} = Astro.props
---

<a class='group sm:flex' href={`/articles/${slug}/`}>
	<div
		class='lg:first:w-1/2 flex-shrink-0 relative rounded-xl overflow-hidden w-full h-[200px] sm:w-[250px] sm:h-[400px]'
	>
		<Image
			src={heroImage}
			width={600}
			height={200}
			format='webp'
			class='h-full min-w-full object-cover hover:scale-[101.5%] transition-all duration-200 rounded-[2px]'
			alt={alt}
		/>
	</div>

	<div class='grow'>
		<div class='p-4 flex flex-col h-full'>
			<div class=''>
				<p
					class='mr-1 mb-1 inline-flex items-center py-1.5 px-3 rounded-md text-xs font-medium bg-slate-200 dark:bg-slate-800 text-slate-800 dark:text-slate-50'
				>
					{category}
				</p>
				{
					tags.map((tag: string) => (
						<p class='mr-1.5 mb-1 inline-flex items-center py-1.5 px-3 rounded-md text-xs font-medium border dark:border-zinc-700'>
							{'#' + tag}
						</p>
					))
				}
			</div>
			<h1 class='mt-1 text-lg sm:text-2xl font-semibold text-gray-800 dark:text-gray-300'>
				{title}
			</h1>
			<p class='mt-2 text-gray-600 dark:text-gray-400'>
				{description}
			</p>

			<div class='mt-auto'>
				<div class='flex items-center'>
					<div>
						<h2 class='text-sm font-semibold text-gray-800 dark:text-gray-200'>
							{issue}
						</h2>
						<p class='text-xs text-gray-700 dark:text-gray-300'>
							{readTime}
						</p>
					</div>
				</div>
			</div>
		</div>
	</div>
</a>
<!-- End Card -->

<script>
	import { animate } from 'motion'

	const images = document.querySelectorAll('img')

	document.addEventListener('DOMContentLoaded', function () {
		const showAnimations = localStorage.getItem('animations') === 'true'

		if (showAnimations) {
			animate(
				'.effect',
				{
					opacity: [0, 1],
					clipPath: ['circle(0% at 0% 0%)', 'circle(100% at 50% 50%)']
				},
				{ duration: 2, easing: 'ease' }
			)
		} else {
			images.forEach((img) => {
				img.classList.remove('opacity-0')
			})
		}
	})
</script>
